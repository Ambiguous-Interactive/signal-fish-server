\* EXPECTED_TLC_FAILURE: Invariant DeletedRoomCannotBeResurrected is violated.
\* A reconnect claim may wait while the room is deleted, but restore must
\* reject afterwards rather than recreate membership or ownership.
SPECIFICATION Spec
CONSTANTS
    Players = {pendingA, pendingB, adopter}
    PendingPlayers = {pendingA, pendingB}
    Adopter = adopter
    Apps = {appA, appB}
    OriginalApp = appA
    OtherApp = appB
    DuplicateOwnerRollbackBug = FALSE
    UseStaleCleanupSnapshotBug = FALSE
    SkipLiveRouteRereadBug = FALSE
    RequeueAfterLaneBug = FALSE
    ResurrectDeletedRoomBug = TRUE
INVARIANTS
    TypeOK
    OwnerRollbackUnique
    RollbackPlacementExact
    CleanupUsesFreshRollbackProvenance
    CleanupCannotRemoveLiveReconnect
    RejectedReconnectRequeuesBeforeLaneRelease
    DeletedRoomCannotBeResurrected
    LiveMembershipExact
    CommittedOwnerStable
    UncommittedOwnerHasRollback
    DeletedRoomIsTerminal
