# AWK Pattern Test: Capitalized Rust

This file tests capitalized Rust code blocks (```Rust).

```Rust
fn capitalized_rust_block() {
    println!("This uses capital R in Rust");
}
```

Expected: Should match and extract this block (case-insensitive).
