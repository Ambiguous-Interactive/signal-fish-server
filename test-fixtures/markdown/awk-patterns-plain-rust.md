# AWK Pattern Test: Plain Rust

This file tests plain rust code blocks (```rust).

```rust
fn plain_rust_block() {
    println!("This is a plain rust block");
}
```

Expected: Should match and extract this block.
